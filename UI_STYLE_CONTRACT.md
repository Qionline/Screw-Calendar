# 日历 UI 样式约束

这份文档是日历 UI 修改前后的回归基准。任何涉及日历网格、工具栏、星期栏、窗口表单或滚动区域的改动，都必须先阅读本文件，并在修改后逐项检查，不能只验证编译结果。

## 修改原则

- 日历网格是一个整体布局：顶部工具栏、星期栏、日期方块、月份标签、外层留白必须一起检查。
- 不得为了修复一个局部对齐问题而改变已经确认的日期方块尺寸、行高、列间距或窗口边距。
- 用户提出百分比调整时，先按当前设计值换算一次，再直接修改固定常量。例如设计值为 `10`，减少 10% 后把常量改为 `9`，不要在运行时写 `10 - 10 * 0.1`。
- 不使用百分比缩放来代替明确的宽度、间距或边距。窗口整体缩放由 WPF Viewbox 负责，日历内部尺寸使用设计坐标中的固定值。
- 若新需求与本文件的既有约束冲突，先调整本文件中的设计基准，再同步修改所有受影响的组件，并说明影响范围。

## 日历固定设计值

以下数值均为日历设计坐标，不能在单个组件中另行计算：

| 项目 | 固定值 | 约束 |
| --- | ---: | --- |
| 日历卡片设计宽度 | 886px | 由日期网格宽度和两侧 16px 内容边距组成 |
| 日期网格左右内容边距 | 16px | 左右必须相同 |
| 日期网格与日期 holder 设计宽度 | 854px | 日期列固定使用；工具栏和星期栏外层按样式扩展 |
| 日期方块宽度 | 118px | 方块本体宽度，不包含 Margin |
| 日期方块四周 Margin | 2px | 上下左右一致，不能用额外列间距替代 |
| 日期列宽 | 122px | `118px + 2px * 2` |
| 日期方块高度 | 105px | 所有日期行一致，不能因月份标签变矮 |
| 月份标签行高度 | 22px | 只增加所在行总高度，不挤压日期方块 |
| 星期栏与日期列 | 7 列固定 122px | 必须和日期方块逐列对齐 |
| 窗口最小宽度 | 300px | 允许高行数日历按屏幕高度等比收窄 |
| 窗口最小高度 | 280px | 仅限制窗口外框，不作为日历组件最小高度参与比例计算 |
| 通用弹窗最外层内边距 | 左右 16px、上下 14px | Markdown 编辑窗口等通用弹窗使用 |
| 设置窗口最外层内边距 | 左 20px、右 16px、上下 14px | 左侧比通用弹窗增加 4px |
| 设置窗口滚动条左间距 | 12px | 比通用滚动条的 8px 增加 4px |
| 日历可见行数 | 最小 5 行、最大 10 行 | 设置滑块、启动归一化和导入归一化统一使用 |

日期网格宽度应为 `7 * 122px = 854px`。工具栏和星期栏可以按外层样式宽度扩展，但其内部星期列仍以 854px 居中对齐。不要把 854px 再乘以 90% 后作为日期列宽度，也不要在满宽容器中把 118px 方块居中来制造新的列间距。

## 对齐关系

- 日期列和每一行日期 holder 使用 854px 设计宽度；经典/极简模式的工具栏与星期栏外层宽度为 878px，紧凑模式为 866px，分别向卡片两侧扩展 12px 与 6px。
- 星期栏和日期网格使用相同的 7 个 122px 列。
- 日期方块只通过固定 2px Margin 保持间距；不得通过父容器剩余空间制造额外左右间距。
- 日期网格首行如果有月份标签，月份标签占 22px 独立行高，日期方块仍保持 105px。
- 修改日期方块宽度时，必须同时检查星期栏、顶部工具栏、月份标签、日期网格左右边距和卡片宽度。

## 表单输入框与 Markdown 编辑器

- Markdown 编辑窗口的最外层内容 `Padding` 固定为左右 16px、上下 14px；设置窗口固定为左 20px、右 16px、上下 14px；1px 外框不计入该数值。
- 文本输入框使用统一模板：圆角、白色背景（深色主题保留主题适配）、浅色上/左/右边框、深灰默认底线。
- 输入框获得焦点时，只将底部横线切换为当前主题色。
- Markdown 编辑窗口使用普通可视块编辑器；工具栏与关闭按钮保持同一行，图片不设置单独的工具栏按钮。
- Markdown 图片通过 `Ctrl+V` 或文件拖入；导入时必须以内容哈希命名并复制到本机数据目录的 `assets` 子目录，Markdown 只引用归档后的相对路径，并按编辑区或待办区域宽度等比缩小。
- 所有竖向滚动条使用固定淡灰蓝色，不跟随主题强调色变成高饱和蓝、绿、紫或橙色；悬停时只适度加深。
- 编辑器粘贴图片时插入当前编辑块之后，并自动滚动到新图片，不能静默追加到视口之外。

## 底部待办面板

- 待办面板位于日历 Viewbox 之外，不能随日历一起等比缩放。
- 日历右下角缩放柄只改变日历宽高；待办面板高度保持不变，宽度只跟随日历的实际显示宽度，不能直接跟随可能更宽的窗口外框。
- 拖动日历右下角时，待办宽度必须在每次尺寸更新中实时重算；不能只在重新渲染待办内容时计算，也不能依靠窗口裁剪形成看似收窄的效果。
- 待办面板底部缩放柄只改变面板高度；日历宽高保持不变。
- 设置页同时提供日历整体大小和待办高度滑块，并说明右下角与底部拖动柄的操作方式。
- 待办面板默认高度为 220px，最小 140px，最大 520px，全部使用固定常量。
- 经典和极简模式下待办宽度等于完整日历卡片显示宽度；紧凑模式下按固定比例与日期内容区对齐。日期列仍使用 `854px` 设计宽度，工具栏和星期栏会按样式向卡片边缘扩展。
- 高行数受屏幕高度限制而缩小日历时，待办必须同步收窄；隐藏待办后不得仅因释放垂直空间而把日历突然放大。
- 今天存在非空 Markdown 时，面板分为左右等宽两栏：左侧常驻待办，右侧今日待办；不得跟随上方日期格的点击或日历翻页。
- 双栏模式正中使用 1px 主题分隔线，左右各保留 12px 间距；单栏模式不显示分隔线。
- 待办标题不显示铅笔编辑按钮；空状态或已有内容的下方正文区域都可以直接进入内联编辑。
- 底部浏览状态使用可选择的只读 Markdown 文档，用户可以选择文本并复制；不得用不可选择的 `TextBlock` 作为完整待办正文。
- 点击空状态、正文或“编辑今日”后，待办面板原地切换为普通 Markdown 编辑器；顶部显示正文、H1—H3、列表、任务工具，最右侧使用无文字的保存图标按钮。
- Markdown 编辑器的整个内容区域都必须可点击聚焦；点击块内容之外的空白区域时，焦点进入最后一个可编辑文本块。
- 今天不存在日期待办时，只显示占满整行的常驻待办区域，不保留空白右栏。
- 底部不再显示新增加号、铅笔和逐条删除按钮；单栏时另提供“编辑今日”。
- 完成 Markdown 任务后，源码中的 `[ ]`/`[x]`、底部列表和日期格摘要必须同步，不能改变文档其他内容的顺序。

## 透明度

- 透明度在经典、极简、紧凑三种模式中都必须作用于日期方块表面、月份标签、Markdown 摘要和待办面板背景。
- 极简模式原本透明的普通日期方块继续透出卡片背景；周末、当天等有色表面必须使用同一透明度。
- 边框和文字保持可读性，不随背景透明度重复衰减。

## 文字清晰度

- 不得把 `DropShadowEffect` 或其他位图特效挂在包含日历文字的根容器上。WPF 会先栅格化整个视觉树，导致 Win11 模式文字发糊。
- 如以后需要阴影，必须新增独立的背景/阴影层，让文字和日期内容保持在无特效的矢量层中。

## 通用窗口层级

- 经典、极简、紧凑三种风格必须使用相同的窗口层级逻辑，风格切换不得改变层级。
- 开启置顶时窗口位于普通窗口上方；全屏窗口覆盖显示器时允许临时覆盖日历，退出全屏后恢复置顶。
- 关闭置顶时窗口必须位于 Windows 桌面层之上、所有普通应用窗口之下。
- 关闭置顶后单击被其他窗口部分遮挡的日历区域，不得使日历越过该普通窗口。
- Windows 10 的“显示桌面”与 Win+D 不得触发任何风格的 `Hide()`；日历需要保持与桌面图标一起可见。
- 托盘主动隐藏不属于“显示桌面”，必须继续有效。
- 实现统一来自 `Shared/DesktopWidgets.Toolkit/Windows/WindowLayerController.cs`，主项目不得复制原生窗口层代码。
- 设置、Markdown 编辑等所属弹窗不继承主窗口的桌面底层位置；用户触发后第一次显示就必须成为前台窗口，关闭后主窗口再恢复原层级。
- 设置与 Markdown 编辑窗口必须非模态显示，打开期间主日历和待办仍可点击；设置按钮再次点击时关闭现有设置窗口，不创建重复窗口。
- 非置顶桌面层打开底部 Markdown 编辑器时，主窗口必须临时允许激活并接收键盘输入；保存或离开编辑器后恢复桌面层的不可激活行为。
- 同一份 Markdown 同时只打开一个编辑窗口；编辑今天的 Markdown 时，底部今日待办实时预览草稿，关闭不保存后恢复已保存内容。

## 每次 UI 修改后的回归清单

1. 日期方块宽度仍为 118px，四周 Margin 仍为 2px。
2. 日期方块高度仍为 105px；有月份标签的首行没有被压缩。
3. 日期 holder 仍为 854px；工具栏和星期栏按当前样式使用 878px 或 866px 外层宽度，日期列中心逐列对齐。
4. 星期列中心与日期列中心逐列对齐，没有新增的外层空白或列间距。
5. 当天高亮、节气、节假日标记和 Markdown 摘要没有因尺寸变化而溢出。
6. 设置窗口、Markdown 可视块编辑器的输入框样式、工具栏和滚动区域没有回归。
7. 完成 Release 构建并确认没有创建 `build-fixed` 等临时项目目录。
8. 开启待办面板后，日历缩放不改变面板高度，面板底部缩放不改变日历宽高。
9. 常驻/今日单栏与双栏切换、今日待办不跟随上方日期点击、完成状态、主题色、深浅主题和桌面挂件透明度没有回归。
10. 三种风格在“显示桌面”后仍可见；关闭置顶后点击日历不会抬到普通窗口上方。
11. 开启置顶后普通窗口不能覆盖日历，全屏窗口可以覆盖，退出全屏后置顶自动恢复。
12. 三种模式的日期表面和待办背景透明度同步；待办宽度始终不超过对应的日历可视宽度。
13. 主窗口关闭置顶且被部分遮挡时，设置与 Markdown 编辑窗口第一次打开就位于遮挡窗口上方。
